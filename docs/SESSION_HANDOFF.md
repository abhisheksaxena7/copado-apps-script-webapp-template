# Session handoff

Update this file when work changes environments, scopes, data contracts, or deployment behavior.

- Current profile:
- Current branch:
- Last passing command:
- Changed files:
- Data/schema decisions:
- OAuth scope changes:
- Script Properties required:
- Stable deployment IDs changed: **No** (explain any exception)
- Human gates still pending:
- Safe next step:

Never paste OAuth credentials, Sheet IDs, owner emails, private `/exec` URLs, or production data into this file.
